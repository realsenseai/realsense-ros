[develop]
script_dir=$base/lib/realsense2_ros_mqtt_bridge
[install]
install_scripts=$base/lib/realsense2_ros_mqtt_bridge

# Tests disabled by default to be skipped on ROS build farm.
# To run locally: pytest test/
[tool:pytest]
collect_ignore_glob = test/*
